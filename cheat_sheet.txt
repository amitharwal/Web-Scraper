// Control Flow
if condition:
    # do something
elif another_condition:
    # do something else
else:
    # fallback

while condition:
    # loop

for item in iterable:
    # loop over iterable

break   # exit loop early
continue  # skip to next loop iteration

// Functions
def my_function(param):
    return param * 2

// Data Structures
# Lists
my_list = [1, 2, 3]
my_list.append(4)

# Dictionaries
my_dict = {'key': 'value'}
my_dict['new'] = 'something'

# Sets
my_set = {1, 2, 3}

# Tuples
my_tuple = (1, 2)

// Built-in Functions
print()
len()
range()
type()
str(), int(), float()

// Exceptions
try:
    risky_code()
except SomeError:
    handle_error()
finally:
    cleanup()

// File I/O
with open('file.txt', 'r') as f:
    content = f.read()

// Importing Modules
import math
from datetime import datetime